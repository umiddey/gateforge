/**
 * Pack identity constants. The GPP/2 handshake pins these values: the
 * `.gateforge.yml` plugin entry MUST declare the same `id` and `version`
 * (see the pack README).
 */

/** Plugin id every webhook pack contribution is pinned to. */
export const PACK_PLUGIN_ID = 'gateforge.pack-webhook';

/** Detector/pack version. */
export const PACK_VERSION = '0.1.0';