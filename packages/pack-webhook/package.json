{
  "name": "@gate-forge/pack-webhook",
  "version": "0.4.0",
  "license": "Apache-2.0",
  "description": "Webhook contract pack: TS-only detector for webhook endpoint patterns (Express router.post('/webhook'|'/hook'|'/callback'), Fastify/Hono equivalents, @webhook/@on decorator) emitting webhook.endpoint resources with signature/replay/retry attributes; loopback example server with HMAC-SHA256 verification, timestamp anti-replay, in-memory delivery dedup, bounded retries to prove five obligation contracts end-to-end.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "README.md",
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@gate-forge/core": "^0.2.0",
    "@gate-forge/plugin-protocol": "^0.2.0",
    "zod": "^4.1.5"
  },
  "publishConfig": {
    "access": "public"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/umiddey/gateforge.git",
    "directory": "packages/pack-webhook"
  },
  "bugs": {
    "url": "https://github.com/umiddey/gateforge/issues"
  },
  "homepage": "https://github.com/umiddey/gateforge#readme"
}
