[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "gateforge-sqlalchemy-detector"
version = "0.1.0"
description = "Gateforge SQLAlchemy CRUD discovery detector (AST only, stdlib only, GPP/3)."
requires-python = ">=3.11"

[tool.setuptools]
packages = ["gateforge_sqlalchemy_detector"]