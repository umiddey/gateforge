"""FastAPI route detection for Gateforge (GPP/3 plugin).

The detector vocabulary is frozen in this module's docstring and mirrored
by ``gateforge_fastapi_detector/scan.py`` (the implementation). See the
pack README and ADR 0004 D1 for the contract-resource semantics.
"""

PLUGIN_ID = "gateforge.pack-fastapi"
VERSION = "0.1.0"
